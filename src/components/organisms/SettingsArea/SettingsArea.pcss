.settings-area {
    width: 100%;
    max-width: 420px;
    margin: 0 auto;
    padding-top: 5em;
    height: 100%;
    box-sizing: border-box;
    display: flex;
    align-items: center;
    flex-direction: column;
    justify-content: flex-start;
    background-color: var(--bg-elem);

    &__user-name {
        margin-top: .5em;
    }
}