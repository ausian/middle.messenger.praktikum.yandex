.messages-bar {
    align-items: center;
    background-color: var(--bg);
    color: var(--color-hide);
    display: flex;
    flex-direction: column;
    height: 100%;
    justify-content: center;
    width: 100%;
}
